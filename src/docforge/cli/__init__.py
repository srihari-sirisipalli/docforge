"""
DocForge — CLI Package
========================

Click-based command-line interface with subcommands:
  docforge scan      — Discover PDFs in a directory
  docforge process   — Run the full extraction pipeline
  docforge rename    — Preview or execute file renames
  docforge organize  — Organize files into folder structure
  docforge export    — Export results to Excel/JSON/CSV/Markdown
  docforge status    — Show processing progress
  docforge rollback  — Undo file operations
  docforge model     — Manage VLM models
  docforge setup     — First-run setup wizard
"""
